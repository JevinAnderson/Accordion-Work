import ctr from './container';
import ctt from './content';
import ctrl from './controller';
import sect from './section';

export const Container = ctr;
export const Content = ctt;
export const Controller = ctrl;
export const Section = sect;

export default {
  Container,
  Content,
  Controller,
  Section
};
